[phases.setup]
nixPkgs = ["maven", "openjdk17"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "sh -c 'JAR=$(find . -name \"placement-management-system-*.jar\" ! -name \"*.original\" | head -n 1); java -Dserver.port=${PORT:-8080} -jar \"$JAR\"'"
