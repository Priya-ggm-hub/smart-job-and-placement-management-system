# ==========================================================
# Smart Job & Placement Management System Configuration
# ==========================================================
spring.application.name=placement-management-system
server.port=${PORT:8080}

# ----------------------------------------------------------
# Database Configuration (Supports Railway MySQL, Spring Boot standard, and custom DB_* env vars)
# ----------------------------------------------------------
spring.datasource.url=${MYSQL_URL:${MYSQL_PRIVATE_URL:${MYSQL_PUBLIC_URL:${DATABASE_URL:${MYSQLURL:${SPRING_DATASOURCE_URL:${DB_URL:jdbc:mysql://${MYSQLHOST:${MYSQL_HOST:${DB_HOST:localhost}}}:${MYSQLPORT:${MYSQL_PORT:${DB_PORT:3306}}}/${MYSQLDATABASE:${MYSQL_DATABASE:${DB_NAME:placement_db}}}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true}}}}}}}
spring.datasource.username=${MYSQLUSER:${MYSQL_USER:${SPRING_DATASOURCE_USERNAME:${DB_USERNAME:${DB_USER:root}}}}}
spring.datasource.password=${MYSQLPASSWORD:${MYSQL_PASSWORD:${SPRING_DATASOURCE_PASSWORD:${DB_PASSWORD:root123}}}}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=30000

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# ----------------------------------------------------------
# File Upload Configuration (Resume PDFs & Bulk CSV/Excel)
# ----------------------------------------------------------
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=${UPLOAD_DIR:uploads/resumes}

# ----------------------------------------------------------
# JWT Security Configuration
# ----------------------------------------------------------
# 256-bit secret key for HMAC-SHA256 signature
app.jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
# 24 Hours validity (in milliseconds = 86400000)
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# ----------------------------------------------------------
# Email (SMTP) Configuration
# ----------------------------------------------------------
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.enabled=${MAIL_ENABLED:false}

# Logging
logging.level.com.placement=INFO
logging.level.org.springframework.security=WARN
